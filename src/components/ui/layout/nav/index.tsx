import React from "react";
import useAlertNotification from "../../../../hooks/useToast";

const Navbar = () => {
	// useAlertNotification({
	// 	message: "Hello World",
	// 	type: "warning",
	// });
	return (
		<div className="flex sticky top-0 z-40 w-full h-10 bg-[]">
			<div className=""></div>
		</div>
	);
};

export default Navbar;
